{
  "statements": [{
    "query": "MATCH (:AWSAccount{id: $AWS_ID})-[:RESOURCE]->(n:AWSRole) WHERE n.lastupdated <> $UPDATE_TAG WITH n LIMIT $LIMIT_SIZE SET n += {deleted: true}",
    "iterative": false,
    "iterationsize": 100
  }],
  "name": "cleanup AWSRole"
}
 