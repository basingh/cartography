{
  "statements": [
    {
      "query": "MATCH (n:AccountAccessKey)<-[:AWS_ACCESS_KEY]-(:AWSUser)<-[:RESOURCE]-(:AWSAccount{id: $AWS_ID}) WHERE n.lastupdated <> $UPDATE_TAG WITH n LIMIT $LIMIT_SIZE SET n += {deleted: true}",
      "iterative": false,
      "iterationsize": 100,
      "__comment__": "cleanup access keys that are attached to users"
    }
  ],
  "name": "cleanup AccountAccessKey"
}
